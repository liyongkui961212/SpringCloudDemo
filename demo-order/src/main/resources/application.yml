server:
  port: 8082

spring:
  application:
    name: order-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/
logging:
  level:
    com.study.order.service.PaymentService: debug

feign:
  hystrix:
    enabled: true
